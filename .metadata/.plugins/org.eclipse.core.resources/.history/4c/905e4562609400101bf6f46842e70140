package inorderTraverserBT;

public class BinaryTree {
	
	public Node preorderTraverser(Node node) {
		if(node==null) {
			return node;
		}
		else {
			System.out.println(node.data);
			preorderTraverser(node.left);
			preorderTraverser(node.right);
		}
		return node;
	}
}
